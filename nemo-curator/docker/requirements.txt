# NeMo Curator Workshop Dependencies
# Pinned to compatible ranges for reproducibility.

# Core data processing
datasets>=2.14.0
pandas>=2.0.0

# Language identification
fasttext-wheel>=0.9.2

# Fuzzy deduplication (MinHash LSH)
datasketch>=1.6.0

# Text cleaning
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Unicode normalization (stdlib, but explicit about intent)
# unicodedata is in stdlib -- no pip package needed

# Hashing
# hashlib is in stdlib -- no pip package needed

# Progress display
tqdm>=4.65.0
